# packages/ui

Future reusable UI components for LocalDocs.

This package intentionally does not include React, Vite, styling frameworks, analytics, telemetry, uploads, accounts, or payment UI. Component implementation should wait until the web app has real product needs.
