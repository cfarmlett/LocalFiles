# packages/ui

Reusable UI components for LocalDocs.

This package contains small shared React components used by the app shell. It intentionally avoids styling frameworks, analytics, telemetry, uploads, accounts, payment UI, and product-specific PDF behavior.

Keep components boring, accessible, and easy to review.
