# packages/pdf

Future PDF adapter interfaces and implementations for LocalDocs.

No PDF implementation is included yet. PDF processing should later be isolated behind explicit interfaces so the rest of the app does not depend directly on a specific PDF library.
