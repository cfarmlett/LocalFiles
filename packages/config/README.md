# packages/config

Future shared project configuration for LocalFiles.

This package is reserved for reusable TypeScript, lint, test, and formatting configuration. It should not introduce runtime dependencies, cloud services, telemetry, analytics, account systems, or product infrastructure.
